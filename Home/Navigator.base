views:
  - type: cards
    name: Navigator
    filters:
      and:
        - file.hasTag("project")
        - file.name != "Tasks"
    order:
      - title
    cardSize: 110
    image: note.image
    imageAspectRatio: 0.95
